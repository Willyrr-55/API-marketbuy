import { ParseFormDataJsonPipe } from './parse-form-data-json.pipe';

describe('ParseFormDataJsonPipePipe', () => {
  it('should be defined', () => {
    expect(new ParseFormDataJsonPipe()).toBeDefined();
  });
});
